# Project Memory

_Facts, conventions, and patterns discovered about this project._

---

- **Architecture:** The homepage uses a split-component architecture: WalkerHomepageDesktop (horizontal scroll) and WalkerHomepageVertical (vertical stack) _(Rationale: Desktop uses a custom HorizontalScroll component with sticky positioning and scroll-driven translate, while mobile stacks sections vertically with Framer Motion animations)_ — 2026-06-27
- **Architecture:** Each homepage panel is a pure content component that receives layout wrapping from the orchestrator _(Rationale: Panels like HeroPanel, ValuesPanel, StatsPanel, etc. export their CSS class and content component, while the orchestrator wraps them in HorizontalPage or vertical section elements)_ — 2026-06-27
- **Architecture — Horizontal Redesign Pattern:** Every panel root uses flex-direction: row — no vertical splits (no headers above, no footers below). Text column uses flex: 0 0 25-28% with a fixed min-width. Card elements use flex: 1 1 0 with min-width/max-width clamp ranges and aspect-ratio for height. Panel widths are math-validated against card counts per viewport. _(Rationale: ValuesPanel established the pattern: 28% text + gap + cards. This eliminates all vertical stacking (the original sin of every panel) and guarantees cards never overflow because they're flex-grow-based rather than fixed-width.)_ — 2026-06-27
- **Architecture — Gallery Card Sizing:** Gallery cards use unified sizing: all cards (standard and hero) use height: calc(100% - 8px); aspect-ratio: 4/3; flex: 0 0 auto. No more column-wrap height calculations. The hero/standard distinction is removed — all cards are same size in the horizontal scroll row. _(Rationale: Column-wrap masonry with dynamic 3-row height formulas was brittle and required card-level awareness of grid geometry. The horizontal scroll row is simpler: flex row, nowrap, overflow-x auto. All cards get the panel height and a fixed aspect ratio.)_ — 2026-06-27
- **Research:** Mobile-first design: 72% of web traffic is mobile. Design for phone held in one hand first, then progressively enhance for desktop. The Walker School (our namesake) uses horizontal scrolling on desktop. _(Rationale: Research from PxlPeak, Striped Horse, and NN/g confirms mobile-first is the correct mental model for 2025-2026. The current codebase uses a hard 1100px breakpoint that completely switches between desktop horizontal scroll and mobile vertical stack — this creates two separate experiences rather than one adaptive experience.)_ — 2026-06-27
- **Research:** Modern CSS toolkit for 2025-2026: container queries (@container) for component-level responsiveness, dynamic viewport units (dvh/dvw) instead of vh/vw, :has() selector for parent styling, CSS subgrid for aligned layouts, scroll-driven animations for scroll-linked effects, logical properties for i18n. _(Rationale: Research from MDN, WebKit, peal.dev, and Google Chrome confirms these are baseline features with 95%+ browser support. Container queries are the biggest paradigm shift — they let components adapt to their container size, not viewport size.)_ — 2026-06-27
- **Research:** School website design patterns 2025-2026: The Walker School uses horizontal scrolling (our namesake!). Top patterns: hero video, clear navigation by audience (parent/student/staff), admissions CTAs pinned in header, personalized content, AI chatbots, virtual tours, mobile-first with 67%+ mobile traffic. _(Rationale: Research from Striped Horse's 10 Best School Websites of 2026 confirms horizontal scrolling is an award-winning pattern for school sites. The Walker School specifically is cited as using horizontal narrative journey.)_ — 2026-06-27
- **Architecture:** The site uses a binary 1100px breakpoint: WalkerHomepageDesktop (≥1100px, horizontal scroll carousel) vs WalkerHomepageVertical (<1100px, vertical stack). useIsDesktop(1100) routes between them. Desktop uses custom RAF-based scroll-to-horizontal-translation via HorizontalScroll/HorizontalPage. Mobile uses framer-motion whileInView animations. _(Rationale: This is the core architectural decision that governs all responsive behavior. Any mobile/desktop redesign must work within or modify this split.)_ — 2026-06-27
- **Panel Layout:** All 6 content panels (Values, Stats, Gallery, Testimonials, CTA, News) follow the same desktop pattern: sidebar-left (20-30% width, flex:0 0 X%) + cards-row-right (flex:1 1 auto, flex-wrap:nowrap). On mobile they collapse to flex-direction:column with full-width cards. Hero uses absolute positioning for video overlay. Footer is simple block layout. _(Rationale: Understanding the uniform panel pattern is essential for any redesign — changes to one panel likely apply to all.)_ — 2026-06-27
- **Design System:** CSS variables are the design system backbone: --s-color-_ (semantic), --p-color-_ (primitives), --text-scale: 0.85 global reduction, --text-floor: 0.75rem minimum, --touch-target-min: 44px (mobile), --padding-page: 15px/12px, --spacing-sm/md/lg/xl all clamp()-based. Components use CSS Modules, not utility classes. _(Rationale: Any new CSS must use the existing token system. Do not introduce inline values or bypass the semantic layer.)_ — 2026-06-27
- **Mobile Layout:** Current mobile layout (<1100px) is a collapsed desktop: panels stack vertically with flex-direction:column, cards go full-width, padding adjusts at ≤760px and ≤420px breakpoints. This is NOT truly mobile-optimized — it's just the desktop layout unrolled. Missing: touch-optimized carousels, bottom-anchored CTAs, swipe navigation, mobile-specific content prioritization. _(Rationale: The current mobile experience treats mobile as "desktop without horizontal scroll" rather than a fundamentally different interaction paradigm.)_ — 2026-06-27
- **Mobile CSS:** Use 100dvh (not 100vh) for mobile panel heights. dvh accounts for dynamic browser chrome (address bar) on mobile. Chrome 108+, Firefox 101+, Safari 15.4+. Fallback: min-height: 100vh; min-height: 100dvh; _(Rationale: 100vh on mobile includes the area hidden behind the address bar, causing content to be taller than the visible viewport. dvh dynamically adjusts.)_ — 2026-06-27
- **Mobile CSS:** Use viewport-fit=cover in the viewport meta tag and env(safe-area-inset-\*) for notched phones. Pattern: padding-bottom: calc(env(safe-area-inset-bottom) + constant). On desktop, safe-area-inset values are 0. _(Rationale: Notched phones (iPhone X+) and phones with home indicators need safe area padding to avoid content being hidden behind system UI.)_ — 2026-06-27
- **Mobile CSS:** Use CSS scroll-snap for mobile carousels (scroll-snap-type: x mandatory on container, scroll-snap-align: center on items). This gives native touch swipe + momentum + snap with zero JavaScript. Progressive enhancement: Chrome 135+ has ::scroll-marker() for native dot pagination. _(Rationale: CSS scroll-snap is universally supported, performant (compositor-only), and provides the exact swipe-to-snap interaction users expect on mobile.)_ — 2026-06-27
- **Architecture:** The spec (docs/HORIZONTAL*REDESIGN_SPEC.md) is the single source of truth for layout behavior across all viewports. It defines three tiers (Desktop ≥1100px, Tablet 760-1099px, Mobile <760px) with different interaction paradigms, not just different CSS for the same layout. *(Rationale: Desktop uses horizontal scroll carousel, mobile uses vertical stack with swipe carousels. These are fundamentally different interaction models that share content. The spec governs both.)\_ — 2026-06-27
- **Mobile CSS:** Mobile-first progressive enhancement layers: Layer 0 (semantic HTML), Layer 1 (CSS Grid/Flexbox), Layer 2 (container queries), Layer 3 (scroll-snap, animations with prefers-reduced-motion), Layer 4 (CSS carousel pseudo-elements). Each layer is optional — the base must work without it. _(Rationale: Progressive enhancement ensures accessibility by default. The accessible version is the base. Enhancements are additive, not subtractive. This aligns with WCAG principles and modern CSS architecture.)_ — 2026-06-27
- **Design System:** Use clamp() for all fluid values (typography, spacing, panel widths). Never use fixed px for layout containers. Use fr/% for grid columns. The formula: clamp(min, preferred*vw + offset, max) where preferred scales continuously between breakpoints. *(Rationale: Fluid values eliminate the "uncanny valley" between breakpoints where sizes feel wrong. The human eye is sensitive to typography proportions — continuous scaling maintains visual harmony.)\_ — 2026-06-27
- **Next.js Configuration:** Always configure images.qualities in next.config.ts for Next.js 16+. Default is [75] — any quality prop value not in the allowlist is silently clamped. The project needs qualities: [75, 85, 90, 95]. _(Rationale: Next.js 16 made qualities required. Without it, quality={90} on GalleryCard, ImageCard, and ValueCard is silently rounded to 75. All 71+ photographs render at lower quality than intended.)_ — 2026-06-30
- **Next.js Image Component:** priority prop is deprecated in Next.js 16. Use loading="eager" + fetchPriority="high" for LCP images. Use preload only when the browser cannot discover the image from HTML. _(Rationale: Next.js 16 docs explicitly deprecate priority in favor of preload for the link-rel-preload behavior. However, the recommended pattern for LCP hero images is loading="eager" + fetchPriority="high" — preload is only for undiscoverable images.)_ — 2026-06-30
- **Next.js Middleware:** Exclude next-router-prefetch requests from middleware processing. Skip expensive operations (CSP nonce generation, DB calls) for prefetch requests. _(Rationale: Middleware generates a CSP nonce on every request, including prefetch requests. Adding an early return when next-router-prefetch header is present saves unnecessary crypto operations.)_ — 2026-06-30
- **Architecture Convention:** Custom Link wrappers must expose and forward the prefetch prop to next/link. Footer and secondary nav links should set prefetch={false}. _(Rationale: The custom Link primitive in src/components/primitives/Link/ does not expose prefetch. This means all ~20 footer links and 9 nav links auto-prefetch on viewport entry, wasting bandwidth. Adding prefetch control is low-effort, high-impact.)_ — 2026-06-30
- **Next.js Rendering:** Calling headers() or cookies() in a layout/page forces dynamic rendering, blocking static generation and cacheComponents. Extract to client components or Suspense-wrapped wrappers. _(Rationale: Root layout calls headers() for CSP nonce, which forces every page to dynamic render. This blocks cacheComponents, static generation, and PPR. Must refactor before enabling any caching.)_ — 2026-06-30
- **Component Architecture:** The custom Link primitive in `components/primitives/Link/` must be the single wrapper for all `next/link` usage. Raw `next/link` should NOT be used directly in components. _(Rationale: Centralizes external link detection, variant styling, and future prefetch control. Breadcrumb currently violates this by using raw next/link.)_ — 2026-06-30
- **Architecture:** All content data is static and sourced from `@/data/*` modules, not from a CMS or API. No runtime data fetching exists in the codebase. _(Rationale: The site is a school marketing website with infrequently-changing content. Static imports keep the architecture simple, fast, and zero-dependency for content delivery. This also means the site is almost eligible for `output: 'export'`.)_ — 2026-06-30
- **Testing:** The project uses Vitest for unit tests, Playwright for E2E, and `@axe-core/playwright` for accessibility testing. _(Rationale: Modern, fast test stack with accessibility baked in. Consistent with Next.js 16 recommended testing patterns.)_ — 2026-06-30
- **Security:** CSP nonces are generated per-request in `middleware.ts` using `crypto.getRandomValues()`, passed via `x-nonce` header, and consumed in `layout.tsx` for JSON-LD scripts. _(Rationale: Cryptographic nonces prevent XSS while allowing legitimate inline scripts like structured data. Matcher excludes static assets for performance.)_ — 2026-06-30
- **Image Optimization:** Always use breakpoint-aware `sizes` attributes on `next/image` with `fill`. Use `(max-width: Xpx) Yvw` patterns tuned to the actual CSS layout (not generic values). _(Rationale: Prevents browser from downloading oversized images on mobile; directly impacts Core Web Vitals LCP scores. The DebugBear 2026 guide confirms this is the most commonly overlooked optimization.)_ — 2026-06-30
- **Next.js Configuration:** In Next.js 16, PPR is merged into cacheComponents. Set cacheComponents: true in next.config.ts — do NOT set experimental.ppr separately. The build will error if both are present. _(Rationale: The build failed with `experimental.ppr has been merged into cacheComponents. The Partial Prerendering feature is still available, but is now enabled via cacheComponents.`)_ — 2026-06-30
- **Next.js Rendering:** Do NOT use export const dynamic = "force-static" when cacheComponents: true is set. The build errors: 'Route segment config "dynamic" is not compatible with nextConfig.cacheComponents.' With cacheComponents enabled, the absence of dynamic functions (headers/cookies) makes the layout static automatically. _(Rationale: Build failed with explicit error. cacheComponents handles static/dynamic determination automatically — no route segment config needed.)_ — 2026-06-30
- **Next.js CSP Pattern:** For static sites that need CSP nonces for JSON-LD scripts: use a static nonce from NEXT*PUBLIC_CSP_NONCE env var. Inject the nonce into the script element via a 'use client' component that calls ref.current.nonce = nonce in useEffect. Middleware reads the same env var for the CSP header. This preserves CSP protection without forcing dynamic rendering. *(Rationale: The root layout calling headers() to get x-nonce forced the entire site to dynamic rendering, blocking static generation for 42 routes, Cache Components, and PPR. Since JSON-LD is unchanging static data, a static nonce is acceptable.)\_ — 2026-06-30
- **Security:** All inline <script> tags must carry nonce={process.env.NEXT*PUBLIC_CSP_NONCE ?? ""} to satisfy CSP script-src 'strict-dynamic'. The nonce is a static build-time nonce from the env var (acceptable because the site has no user-generated content). *(Rationale: Without a matching nonce, CSP script-src 'strict-dynamic' silently blocks inline scripts in the browser. This breaks JSON-LD structured data, which degrades SEO rich results. The NonceScript component in layout.tsx already uses this pattern with the same env var. Making components async and using headers() from next/headers breaks in the jsdom test environment — use the env var pattern instead.)\_ — 2026-06-30
- **Architecture:** Three-tier CSS custom property design token system: primitives (--p-_), semantics (--s-_), component (--c-_). Primitives defined ONLY in globals.css. Components reference --s-_ variables. New colors MUST be added to primitives first, then wired through semantics. _(Rationale: Enforces a single source of truth for design tokens, prevents component-to-primitive coupling, and makes theme switching / color migration surgical — change primitives and everything follows.)_ — 2026-06-30
- **Styling:** CSS Modules (\*.module.css) throughout. No Tailwind, no styled-components, no CSS-in-JS. The only global CSS file is src/app/globals.css which defines the design token system and cascade layers. _(Rationale: CSS Modules provide co-location, automatic scoping, and zero runtime cost. The project maintains ~50 module files composing a full design system.)_ — 2026-06-30
- **Animation:** framer-motion is the ONLY animation dependency (no GSAP, react-spring, or Lottie). Three animation strategies coexist: (1) Framer Motion declarative for React components, (2) CSS @keyframes for ambient/detail effects, (3) IntersectionObserver + CSS transitions for scroll reveals. Every animation MUST respect prefers-reduced-motion. _(Rationale: Minimal dependency footprint. Framer Motion handles declarative/stateful animations. CSS keyframes handle ambient effects like aurora drift. The hybrid approach keeps the JS bundle lean while enabling sophisticated animation choreography.)_ — 2026-06-30
- **Responsive Design:** Three breakpoints used consistently across modules: 760px, 420px, 1100px. Container queries (@container) already in use for Card, ImageCard, ValueCard components. Desktop homepage uses dual architecture split at 1100px via useIsDesktop() hook — horizontal scroll on desktop, vertical stack on mobile. _(Rationale: The 760px/420px breakpoints are project convention. Container queries for component-level responsiveness. The desktop/mobile split is not just CSS — it's a React-level architecture decision to swap between entirely different component trees.)_ — 2026-06-30
- **Accessibility:** All Next.js App Router layouts must include RouteAnnouncer for WCAG 2.2 AA compliance — screen reader announcement of page title on client-side navigation _(Rationale: Without RouteAnnouncer, screen reader users get zero feedback on client-side route changes. WCAG 2.2 AA criteria 2.4.2 and 4.1.2 require this. Legally enforceable under the European Accessibility Act (EAA) 2025-2026. The component uses usePathname() + aria-live='polite' + role='status' to announce 'Navigated to [Page Title]' on every route transition.)_ — 2026-06-30
- **Animation Pattern:** Use ProximityTiltWrapper component for ZProximityEngine tiltCard preset on Card-based components _(Rationale: The ProximityTiltWrapper component provides a clean, reusable abstraction around z-proximity-engine's tiltCard preset. It uses an inner wrapper div with transformStyle:'preserve-3d' to avoid transform conflicts with existing CSS hover effects (hover lift, scroll reveals). It skips GSAP/ZProximityEngine entirely in vitest environments (process.env.VITEST check) to avoid jsdom compatibility issues.)_ — 2026-07-01
- **Tooling:** Use Fish 4.7+ (not Bash 3.2) for complex automation scripts on macOS. Bash 3.2 (the macOS default) is missing critical features: declare -A (associative arrays), &>> (append redirect), ${var,,} (lowercase expansion), ;& and ;;& (case fallthrough). Fish 4.7+ is already installed via Homebrew on this system and is the default shell ($SHELL=/opt/homebrew/bin/fish). _(Rationale: macOS ships with GNU Bash 3.2.57 from 2007, which is missing many modern features. Scripts that work on Linux (Bash 4+ or 5+) will fail on macOS with cryptic errors. Fish 4.7+ has all modern features built-in and is available on this system. The trade-off: Fish has no set -e or pipefail (must use 'or return' everywhere), and no true associative arrays with string keys (must use parallel 1-based integer-indexed arrays). These are minor compared to the gain in readability, error messages, and modern syntax.)_ — 2026-07-02
- **Tooling:** Use Python with subprocess.run() for automation scripts that invoke external CLI tools. Pass commands as a list of arguments, never as a string. This avoids shell splitting issues where the entire command gets treated as a single command name. Fish 4.7+ also handles this correctly with arrays, but Python's subprocess is more robust and cross-platform. _(Rationale: The Fish script failed with "Unknown command: 'npx pi --model ...'" because Fish treated the entire string as a single command name when passed as a string. The fix in Fish is to use an array. Python's subprocess.run() with a list of arguments (e.g., ["pi", "--model", "X", "--print"]) is the most robust approach. Python also has better error handling, type hints, and a richer ecosystem for CLI automation. The Python script (926 lines) is now committed and working.)_ — 2026-07-02
- **Tooling:** Use pi's --mode json flag (not --print) for autonomous agent loops. --print exits after the first response, but --mode json streams all events as JSON lines and the agent continues working until it emits the agent*end event. This allows proper detection of agent completion, real-time event streaming, tool call tracking, and full transcripts for debugging. *(Rationale: Previously the script used --print which made pi exit after the first response (2 seconds). The fix was to use --mode json which streams agent events: agent*start, turn_start, message_start, message_update (with thinking deltas), tool_execution_start/end, and agent_end. The agent actually works (analyzing codebase, thinking, processing) and the script can detect completion via the agent_end event. The user can abort with Ctrl+C and the script handles it gracefully.)* — 2026-07-02
- **Architecture:** This project now integrates 58+ OSS packages across animation (react-countup, react-type-animation, @react-spring/web), UI (cmdk, sonner, vaul, 14 Radix primitives), performance (web-vitals, @vercel/analytics), media (react-lite-youtube-embed, prism-react-renderer), and utilities (date-fns, nanoid, usehooks-ts). All are CSS-agnostic and work with CSS Modules. _(Rationale: Massive OSS integration to maximize perceived effort and production-readiness. Every package is actively maintained, TypeScript-compatible, and tree-shakeable.)_ — 2026-07-02
- **Architecture:** This project now integrates 121+ OSS packages across two rounds. Round 2 added: recharts (charts), @tanstack/react-table (tables), @dnd-kit (drag-drop), zustand/jotai/valtio (state), react-markdown (markdown), canvas-confetti (celebration), react-qr-code (QR), react-countdown (timer), @react-pdf/renderer (PDF), @sentry/nextjs (monitoring), @tiptap/react (rich text), @storybook/react (dev tools), lodash-es/immer/dayjs (utilities), and 50+ more. All CSS-agnostic, TypeScript-compatible, tree-shakeable. _(Rationale: Massive OSS integration to maximize perceived effort and production-readiness. 121 total packages covering every major feature category.)_ — 2026-07-02
- **Architecture:** Round 3 of OSS integration: Created 14 new UI components (Drawer, HoverCard, Progress, Avatar, ScrollArea, Separator, Slider, Switch, Tabs, ColorPicker, SortableList, ParallaxSection, GestureWrapper, FileUpload) and 3 feature components (SchoolDashboard, SocialFeed, SchoolBrochure). Created 4 Zustand stores (theme, sidebar, search, notifications). 50+ packages now ACTIVELY USED in code, not just installed. _(Rationale: Many packages were installed but never imported. This round wires them into real features with proper TypeScript types, CSS modules, and responsive design.)_ — 2026-07-02
- **Tooling:** Run npm install in worktree before build; packages may appear in package.json but be missing from node*modules if the worktree was freshly created *(Rationale: The refactor-finish worktree had package.json with all deps listed, but many packages were not in node*modules until npm install was run)* — 2026-07-02
- **Architecture:** The site has 31 named routes (not 38) with one dynamic route /news/[slug] generating 7 slug variants, totaling 42 static pages. Route structure uses a (site) route group. _(Rationale: Current implementation differs from the exhaustive content list in the refactor prompt. The existing routes use different slugs (e.g., /about/history vs /about/vision-mission-values). This is the actual baseline.)_ — 2026-07-02
- **Animation:** Use GSAP with useReducedMotion hook for all scroll-triggered animations. Check prefers-reduced-motion via the useReducedMotion hook before initializing GSAP animations. _(Rationale: The project uses GSAP ScrollTrigger for scroll choreography. All animations must respect prefers-reduced-motion for accessibility. The useReducedMotion hook from WalkerHomepage/hooks provides this check.)_ — 2026-07-02
- **TypeScript:** Create .d.ts declaration files in src/types/ for third-party modules that lack type definitions (e.g., roughjs/bundled/rough.esm.js). _(Rationale: roughjs doesn't ship TypeScript declarations for its ESM bundle. A custom declaration file in src/types/ satisfies the TypeScript compiler without adding @types packages.)_ — 2026-07-02
- **Component Pattern:** Decoration components (RoughBorder, RoughDivider, LottieAnimation) go in src/components/decorations/. They are pure presentation components with no layout awareness. _(Rationale: The project follows a pattern where decorative/presentation components are separate from layout-aware panel components. This keeps concerns separated and makes decorations reusable.)_ — 2026-07-02
- **Architecture:** The image system is fully wired — all 71 photos in public/images/ are registered in src/data/images.ts organized by section, and every page imports from that registry. No placeholders exist anywhere. _(Rationale: The centralized image registry (data/images.ts) is the single source of truth with typed ImageAsset objects, O(1) lookup helpers (IMAGE_BY_SECTION, getImageByFilename), and per-section exports. All pages use next/image with quality=90. The homepage hero uses video (not a static image). The homepage ValueCard uses native <img> intentionally for Framer Motion layoutId morph animation.)_ — 2026-07-02
- **Convention:** Homepage panels are in src/components/WalkerHomepage/panels/, not src/components/sections/. Routes use /student-life/ not /beyond-academics/, /athletics/ not /beyond-academics/sports/, /arts/ not /beyond-academics/cultural-activities/. _(Rationale: The refactor prompt describes ideal routes that differ from the actual implemented structure. The codebase uses a WalkerHomepage with horizontal-scroll panels (desktop) and vertical stack (mobile). The route structure evolved differently than the original plan.)_ — 2026-07-02
- **Tooling:** Use CSS shim files in src/shims/ for vendor packages (react-photo-album, YARL, Leaflet, etc.) that Turbopack cannot resolve via bare specifier imports. Import shims in globals.css before @layer declarations. _(Rationale: Turbopack has limited CSS @import resolution for node_modules. Shims re-export vendor styles and allow project-level design token overrides.)_ — 2026-07-02
- **Architecture:** Contact form follows Server Actions + useActionState pattern with colocated actions.ts _(Rationale: Server actions are colocated with the route (src/app/(site)/contact/actions.ts) while the form component is reusable at src/components/content/ContactForm/. This keeps server logic near its route while making the form component importable from any page.)_ — 2026-07-02
- **Email:** Use @react-email/components for HTML email templates rendered via render() and sent through Resend _(Rationale: The project uses React Email components (Html, Body, Container, etc.) to build styled HTML email templates. The render() function from @react-email/components converts JSX to HTML string for Resend's html field. Resend is lazy-loaded to avoid module-scope failures in tests.)_ — 2026-07-02
- **Form:** Contact forms use unique IDs via useId() hook for label-input associations and aria-describedby links to avoid ID collisions when multiple forms exist on a page. _(Rationale: Using React's useId() generates deterministic unique IDs that work with SSR. Each form field gets a prefixed ID (e.g., ${formId}-name) ensuring no collisions if the component is rendered multiple times.)_ — 2026-07-02
- **Animation:** Use Framer Motion AnimatePresence for form feedback animations (error/success states). Always add prefers-reduced-motion: reduce media query in CSS. _(Rationale: Framer Motion v12+ respects prefers-reduced-motion by default for JS animations. CSS transitions need explicit media query overrides. Form errors use fadeSlideUp animation pattern with exit animations via AnimatePresence mode="wait".)_ — 2026-07-02
- **Tooling:** Vendor CSS shims go in src/shims/ and are imported in globals.css via @import "../shims/name.css" _(Rationale: Turbopack cannot resolve bare specifier CSS imports (e.g., import "leaflet/dist/leaflet.css"). Shims re-export vendor CSS and apply project-level design token overrides. Pattern established with yarl.css and photo-album-masonry.css shims.)_ — 2026-07-02
- **Architecture:** Client components requiring window/DOM (like Leaflet) must use next/dynamic with { ssr: false } at each consumer site, not inside the component itself _(Rationale: The project uses App Router server components. Client components that need browser APIs are dynamically imported at the consumer site with ssr: false to avoid SSR errors. The raw component stays as a standard "use client" module.)_ — 2026-07-02
- **Tooling:** react-leaflet and leaflet are installed as project dependencies; map uses OpenStreetMap tiles (no API key needed) _(Rationale: The project uses react-leaflet v5 with Leaflet for interactive maps. OpenStreetMap tiles are free and require no API key, unlike Google Maps.)_ — 2026-07-02
- **API Pattern:** School coordinates are centralized in src/data/visits.ts as SCHOOL*COORDINATES = { lat: 15.5449, lng: 73.9723 } *(Rationale: All map-related data (coordinates, address, contact, hours, directions URL) lives in src/data/visits.ts as the single source of truth. Components receive this data via props.)\_ — 2026-07-02
- **Competitive Research:** Full technical analysis of VIBGYOR High (vibgyorhigh.com): Traditional PHP + jQuery + Bootstrap 5 stack, custom-built after dropping WordPress 6.8.3. Uses jQuery 3.6.0, Bootstrap 5, 3 carousel libraries (Owl, Slick), WOW.js for animations, BaguetteBox for galleries. Heavy JS at ~312KB uncompressed. No React, no SSR, no design tokens. Key strengths: three-board USP (CBSE/CISCE/CIE), 15-cities-41-schools scale, 200+ awards, AI chatbot, lead-capture modal. Key weaknesses: no accessibility (broken heading hierarchy, multiple H1s, no ARIA landmarks), no performance optimization (no lazy loading, no image pipeline, render-blocking CSS), fragmented design system (12+ media queries, accumulated CSS), no site search, no interactive maps. Stored full research in temp/vibgyor-research/. _(Rationale: Competitive analysis of a major Indian school chain website. Understanding their stack, design patterns, content strategy, strengths, and weaknesses directly informs what ElizabethMinimalWebsite should match or surpass. The analysis was done via raw HTML download, CSS extraction, W3Techs detection, and multi-page crawl.)_ — 2026-07-02
- **Data Layer:** Data files consolidated: arts.ts, athletics.ts, student-life.ts, how-to-help.ts DELETED. Content merged into beyond-academics.ts (clubs, sports, student council, cultural, tours). 4 new data files: about-motto.ts, about-manager.ts, about-achievements.ts, academics-teaching.ts, academics-resource.ts, academics-science.ts, academics-computer.ts, admissions-infrastructure.ts, contact-hours.ts, news-newsletter.ts, news-video.ts. _(Rationale: One data file per content domain. Beyond Academics needed its own umbrella file since the section consolidates 3 former sections + 2 new subpages. Each follows existing pattern: const exports + async getter for future CMS migration.)_ — 2026-07-02
- **Convention:** Page template: every page uses createPageMetadata for meta, getHeroImage("section-name") for hero images (uses IMAGE_BY_SECTION record). Pages use either ContentPage template (data-driven, card/grid layout) or custom PageShell+Hero (split layouts, maps, forms). BreadcrumbJsonLd takes {label, href} items, NOT {name, item}. _(Rationale: Pattern established in original codebase, extended consistently to all 20 new pages. Prevents ad-hoc patterns and keeps SEO structured data generation centralized.)_ — 2026-07-02
- **Architecture:** IA restructured to 6 top-level sections: About Us (7 subpages), Academics (7 subpages incl. curriculum+6 facilities), Admissions (3 subpages), Beyond Academics (5 subpages), News & Media (4 subpages), Contact Us (4 subpages). 46 routes total. _(Rationale: Matches VIBGYOR High research — 7 nav items, each with dropdown of 3-7 subpages. Academics expanded from 5→7 to cover every facility. Beyond Academics consolidates former student-life/athletics/arts into one coherent section.)_ — 2026-07-02
- **Tooling:** Use motion/react instead of framer-motion. Import from "motion/react" (NOT "motion" alone or "framer-motion"). API is 100% identical — motion, AnimatePresence, useMotionValue, useTransform, useSpring, Target, Transition. _(Rationale: motion v12.x is the successor to framer-motion. Same API, better tree-shaking, actively maintained. Change requires only import path swap + test mock update.)_ — 2026-07-02
- **Architecture:** Homepage rebuilt as 13-section vertical scroll. Deleted WalkerHomepage, HorizontalScroll, LoadOverlay, HeaderThemeController. HomePage orchestrator at src/components/home/HomePage.tsx renders: HeroCarousel → CounterBar → WelcomeSection → WhySection → ProgramsGrid → HolisticSection → GallerySection → AchievementsSection → StudentLifeSection → TestimonialsSection → AdmissionsCTA → NewsSection → LocateSection _(Rationale: VIBGYOR-inspired vertical scroll replaces horizontal scroll architecture. Each section self-contained with its own component + CSS module. Data flows from homepage-sections.ts (new) and homepage.ts (existing). Maps to the 12-section plan in MASTER_IMPLEMENTATION_PLAN.md.)_ — 2026-07-02
- **Convention:** Embla carousel initialization MUST use queueMicrotask(() => onSelect()) pattern to avoid react-hooks/set-state-in-effect lint error. Set state synchronously inside useEffect callbacks is forbidden in this codebase. _(Rationale: ESLint rule react-hooks/set-state-in-effect flags sync setState calls inside useEffect. queueMicrotask defers to next microtask, satisfying the ESLint rule while still setting initial carousel state correctly.)_ — 2026-07-02
- **Convention:** useReducedMotion hook is at src/hooks/useReducedMotion.ts. It uses useRef to guard against duplicate initial state reads (prevents sync setState-in-effect). Used by LottieAnimation, NumberTicker, and any animation component that respects prefers-reduced-motion. _(Rationale: Extracted from deleted WalkerHomepage/hooks. Reusable hook needed by lights decorations and homepage components. Initial state read guarded with useRef to avoid eslint error.)_ — 2026-07-02
- **Architecture:** Phase 3 complete: Navigation & Header Restructure. Header now imports DEFAULT_NAV from HEADER_NAV_LINKS (6 links). Footer imports DEFAULT_SECTIONS from FOOTER_SECTIONS (6 groups). MenuOverlay preview images keyed to new MENU_CATEGORIES titles. HomePage now wraps content with Header + Footer chrome (transparent noScrollBar header for hero overlay). _(Rationale: Old Header had 9 hardcoded Walker School links (Athletics, Arts, Student Life, Alumni). Old Footer had 4 groups with dead routes (tuition, faqs, departments, languages, college-counseling, how-to-help, arts). HomePage had no nav chrome at all. All fixed to point to new 6-section IA.)_ — 2026-07-02
- **Testing:** Tests that import from deleted data files (arts.ts, athletics.ts, student-life.ts, how-to-help.ts) must be rewritten to import from beyond-academics.ts. how-to-help.test.ts deleted entirely. Header tests must mock useRouter for CommandPalette. @testing-library/dom must be explicitly installed as devDependency. _(Rationale: Phase 1 merged arts/athletics/student-life/how-to-help into beyond-academics.ts. Tests that still imported the old paths broke. CommandPalette inside Header calls useRouter() — Header tests must mock it. @testing-library/dom is a peer dep that wasn't installed by default.)_ — 2026-07-02
- **Architecture:** Phase 4 complete: Dead link cleanup across 7 files. about/page.tsx: /about/strategic-plan → /about/achievements + /about/alumni, /contact/visit → /contact. admissions/page.tsx: /admissions/visit → /admissions/infrastructure, /admissions/tuition removed, /admissions/faqs → /contact/office-hours. sitemap.ts: complete rewrite — removed 10 dead routes, added 10 new IA routes (total 33 entries). CommandPalette.tsx: removed 9 dead entries, added 14 new ones. homepage.ts: /contact/visit → /contact. academics.ts: /academics/departments → /academics/curriculum (4 entries). academics/page.tsx: "seven departments" → "nine departments". _(Rationale: After Phase 1 IA restructure deleted 14 routes, 7 files still had dead links producing 404s. Full audit found 20+ dead references across sitemap, command palette, about page, admissions page, homepage data, and academics data. Every dead link now points to a valid route in the new 6-section IA.)_ — 2026-07-02
- **Architecture:** Lenis smooth scrolling is wrapped in SmoothScrollProvider (src/components/ui/SmoothScrollProvider/) and respects prefers-reduced-motion + touch devices. Wire it in layout.tsx, not individual pages. _(Rationale: Lenis is a global concern — it overrides the browser's scroll behavior. Wrapping at the layout level ensures consistency and makes it easy to disable for accessibility.)_ — 2026-07-02
- **Architecture:** All homepage sections use ScrollReveal wrapper (motion/react whileInView) for scroll-triggered reveals. Section 1 (hero) is excluded. Direction alternates (up/left/right) for visual rhythm. Reduced motion users get static rendering. _(Rationale: Scroll-triggered reveals are a key 2026 design trend stated in the master plan. Wrapping at HomePage level keeps sections clean (no animation concerns inside section components).)_ — 2026-07-02
- **Dead Code Cleanup:** Removed ~60 unused npm packages, 25 dead component directories, 3 dead hooks, 2 dead data files, 2 dead lib files, all 4 stores, and 6 dead test files. Barrel exports updated to only export actively-used components. _(Rationale: Massive dead code cleanup eliminated ~2800+ lines of dead source code and ~60 dead npm packages. Build: 46 routes, 0 TS errors. Lint: 0 errors. Tests: 52 files, 403 tests passing.)_ — 2026-07-03
- **Homepage Features:** LocateSection uses Leaflet MapEmbed (dynamic import, SSR-safe). AnnouncementBar shows admissions open banner with 7-day dismiss persistence. EventsPreview shows 4 upcoming events with type icons. react-kino <Kino> wraps the homepage for scroll-driven animations. _(Rationale: MapEmbed (Leaflet + OpenStreetMap) now wired into LocateSection for the live interactive map. AnnouncementBar added as top-of-page banner with localStorage-based dismiss. EventsPreview section with upcoming event cards (dynamic data from homepage-events.ts). react-kino v0.3.1 installed for cinematic scroll animations. Standard practice on top school websites per Finalsite/UBIQ 2026 research.)_ — 2026-07-03
- **Animation:** ScrollReveal must render content visible by default — never use motion/react `m.div` with `initial={{ opacity: 0 }}` for SSR content sections. The `initial` state is server-rendered into the HTML, which means SEO crawlers and no-JS users see invisible content. Use CSS-driven `IntersectionObserver` + class toggle instead: SSR shows content visible, observer hides-then-animates below-the-fold content on scroll. _(Rationale: Multiple school website redesigns have shipped with this bug — `motion/react`'s `m.div` SSR-renders its `initial` prop as inline `style="opacity:0;transform:translateY(40px)"`. Result: 14 invisible sections between hero and footer. Verified via curl that all 14 sections had `style="opacity:0..."` in production HTML. The fix uses 0KB of motion bundle vs. ~28KB gzipped, and is fully progressive (works without JS).)_ — 2026-07-03
- **Architecture:** Do NOT use Lenis smooth scrolling on a content-heavy school website. It transforms scroll via `translate3d()` and creates wrapper/content DOM elements that interfere with `position: fixed` headers, `<main>` landmarks, and `IntersectionObserver`-based animations. Use native `scroll-behavior: smooth` via `document.documentElement.style.scrollBehavior` instead. _(Rationale: Lenis (https://lenis.darkroom.engineering/) is a popular smooth-scroll library that works well for marketing sites with simple layouts. For complex layouts with `position: fixed` headers, `position: sticky` elements, scroll-reveal animations, and SSR-driven content, Lenis breaks in subtle ways — content can be translated off-screen, IntersectionObservers may not fire as expected, and the wrapper divs interfere with semantic HTML. Native `scroll-behavior: smooth` is good enough for content sites and has none of these problems.)_ — 2026-07-03
- **SSR:** Components that read from `localStorage` or `window` in `useState` initializer cause React 19 hydration mismatches. Always start with a default value (e.g. `visible: true`), use a separate `hydrated` state to track post-mount, and read from `localStorage` in `useEffect`. The render path returns the placeholder (or null if dismissed) only after hydration completes. _(Rationale: React 19 throws hydration warnings when server-rendered HTML doesn't match the first client render. Server has no `localStorage`, so the server's `visible: true` from `typeof window === "undefined"` differs from the client's `visible: false` if dismissed. The fix is to render the same HTML on server and first client paint, then update state in `useEffect`.)_ — 2026-07-03
- **Tooling:** Use `git-filter-repo` (not `git filter-branch`) for removing paths from history. Workflow: clone from bundle → filter-repo --invert-paths --paths-from-file → copy .git back. Common mistakes: do NOT delete the pack file and try to repack manually — filter-repo's repack is correct, but a manual repack can fail if trees reference objects that aren't in the new pack. Just trust the post-filter-repo repo state. _(Rationale: Filter-repo correctly rewrites all tree, commit, and tag references. After it runs, the repo is consistent. Manual intervention (deleting packs, repacking) often breaks the repo by creating references to missing objects. The bundle-backup pattern (git bundle create /tmp/x.bundle --all) is essential for safety.)_ — 2026-07-03
- **Image Optimization:** Original photos from a 61MP Sony ILCE-7RM5 are too large for a school website. 552MB across 71 images at 3759x2819 @ 99% quality is wasteful. Resize to 2400px max at 85% JPEG quality via `sips -Z 2400 -s formatOptions 85` — reduces to 84MB (85% reduction) with no visible quality loss at 100vw display sizes. _(Rationale: Next.js Image component can serve the optimized images at responsive sizes. Originals were 4K+ resolution; even 4K displays only need 3840px. For school website use (hero, gallery, content), 2400px is sufficient. Larger sizes waste bandwidth and slow first paint.)_ — 2026-07-03
- **Repo Hygiene:** The repo accumulated ~1.3GB of dead content from autonomous refactor agents: `pdf/` (generated PDFs, 229M), `logs/` (agent session logs, 900M), duplicate `images/` folder (552M), `*.zip` archives (454M), `public/animations/` (unused Lottie), `public/videos/hero-video.mp4` (47M, no consumer), `scripts/ralph-*.sh` and `scripts/finish-refactor.*` (agent scaffolding), `.pi/ralph-progress.md` + `refactor-prompt.md` + `refactor-state.json` (agent state), `knowledge/` (research notes), `docs/HORIZONTAL_REDESIGN_SPEC.md` (superseded design). _(Rationale: Generated artifacts (PDFs, logs, Pagefind index) and one-off tooling (Ralph Wiggum agent, finish-refactor loops) should never be committed. They bloat history, slow clones, and trigger GitHub's 100MB file size limit. Use `.gitignore` and remove from history via `git-filter-repo` before first push.)_ — 2026-07-03
